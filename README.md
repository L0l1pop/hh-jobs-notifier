# 🤖 HH Jobs Notifier Bot

Telegram-бот для автоматического мониторинга вакансий на HeadHunter.ru с мгновенными уведомлениями.

## 📋 Описание

This is my pet project — an automated job notification system that monitors HeadHunter.ru vacancies and sends instant alerts to users via Telegram. Built with modern microservices architecture using Python, FastAPI, aiogram 3.x, and Docker to demonstrate practical skills in backend development and system design.

## ✨ Возможности

- 🔍 **Умный поиск** — настраиваемые фильтры по ключевым словам, городу, опыту и зарплате
- 🔔 **Автоматические уведомления** — получайте новые вакансии каждые 15 минут
- 📊 **Статистика** — отслеживайте количество найденных вакансий
- 🗂 **Управление подписками** — легко добавляйте, удаляйте и приостанавливайте подписки
- 📱 **Удобный интерфейс** — интуитивная навигация через клавиатуры и inline-кнопки

## 🛠 Технологии

- **Python 3.10+**
- **aiogram 3.x** — асинхронный фреймворк для Telegram ботов
- **FastAPI** — для REST API между сервисами
- **Celery + Redis** — фоновые задачи и периодический парсинг
- **PostgreSQL** — хранение данных
- **SQLAlchemy 2.0** — ORM с async поддержкой
- **Docker** — контейнеризация приложения
- **HeadHunter API** — официальный API для получения вакансий
## 📁 Структура проекта

hh-jobs-notifier/
├── bot/ # Telegram бот
│ ├── handlers/ # Обработчики команд
│ ├── keyboards/ # Клавиатуры
│ ├── states/ # FSM состояния
│ └── config.py # Конфигурация
├── database/ # База данных
│ ├── models.py # Модели SQLAlchemy
│ └── database.py # Подключение к БД
├── parser/ # Парсер HH API
│ ├── hh_client.py # Клиент для API
│ └── vacancy_service.py # Сервис вакансий
├── tasks/ # Celery задачи
│ └── vacancy_checker.py # Проверка новых вакансий
├── celery_app.py # Конфигурация Celery
├── logging_config.py # Настройка логирования
├── docker-compose.yml # Docker конфигурация
└── requirements.txt # Зависимости

## 🚀 Установка и запуск

### 1. Клонирование репозитория

git clone https://github.com/your-username/hh-jobs-notifier.git
cd hh-jobs-notifier


### 2. Создание виртуального окружения

python -m venv .venv
source .venv/bin/activate # Linux/Mac
или

.venv\Scripts\activate # Windows


### 3. Установка зависимостей

pip install -r requirements.txt


### 4. Настройка переменных окружения

Создайте файл `.env`:

Telegram

BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_id
Database

DB_HOST=localhost
DB_PORT=5432
DB_NAME=hh_jobs_bot
DB_USER=postgres
DB_PASSWORD=your_password
Redis

REDIS_HOST=localhost
REDIS_PORT=6379
HH API

HH_API_URL=https://api.hh.ru


### 5. Запуск базы данных и Redis

docker-compose up -d


### 6. Запуск приложения

Откройте 3 терминала:

**Терминал 1 — Telegram бот:**

python bot/main.py

**Терминал 2 — Celery Worker:**

celery -A celery_app worker --pool=solo --loglevel=info


**Терминал 3 — Celery Beat:**

celery -A celery_app beat --loglevel=info


## 📖 Использование

1. Найдите бота в Telegram и отправьте `/start`
2. Нажмите **➕ Добавить подписку**
3. Укажите параметры поиска (ключевые слова, город, опыт, зарплата)
4. Получайте уведомления о новых вакансиях автоматически!

## 🎯 Возможности для развития

- [ ] Добавить поддержку других job-сайтов (SuperJob, Rabota.ru)
- [ ] Telegram Mini App для расширенного управления
- [ ] Экспорт вакансий в CSV/Excel
- [ ] Аналитика и графики по вакансиям
- [ ] Уведомления через email
- [ ] ML для рекомендаций вакансий